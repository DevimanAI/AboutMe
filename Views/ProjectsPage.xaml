<UserControl x:Class="AboutMe.Views.ProjectsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="📦 Projects Highlights" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>

                <!-- Project 1: TivaGate -->
                <Border BorderBrush="#2D2D30" BorderThickness="0.5" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="TivaGate" Foreground="White" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock Text="Tech Stack: C#, WPF, SQL Server, Device Integration" Foreground="#A0A0A0" FontSize="14" Margin="0,5,0,10"/>
                        <TextBlock Text="A custom gate management and logging system with full database and device integration." Foreground="#D0D0D0" FontSize="16" TextWrapping="Wrap"/>
                        <!-- Add Screenshot Placeholder/Area here later -->
                    </StackPanel>
                </Border>

                <!-- Project 2: Gym CRM -->
                <Border BorderBrush="#2D2D30" BorderThickness="0.5" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Gym CRM" Foreground="White" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock Text="Tech Stack: C#, WPF, SQL Server, Biometric Integration" Foreground="#A0A0A0" FontSize="14" Margin="0,5,0,10"/>
                        <TextBlock Text="CRM software for managing members, check-ins, and payments with biometric login." Foreground="#D0D0D0" FontSize="16" TextWrapping="Wrap"/>
                        <!-- Add Screenshot Placeholder/Area here later -->
                    </StackPanel>
                </Border>

                <!-- Project 3: Hotel Management System -->
                <Border BorderBrush="#2D2D30" BorderThickness="0.5" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Hotel Management System" Foreground="White" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock Text="Tech Stack: C#, WPF, SQL Server" Foreground="#A0A0A0" FontSize="14" Margin="0,5,0,10"/>
                        <TextBlock Text="Lightweight reservation system with multi-branch support." Foreground="#D0D0D0" FontSize="16" TextWrapping="Wrap"/>
                        <!-- Add Screenshot Placeholder/Area here later -->
                    </StackPanel>
                </Border>

                <!-- Project 4: Library Management System -->
                <Border BorderBrush="#2D2D30" BorderThickness="0.5" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Library Management System" Foreground="White" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock Text="Tech Stack: C#, WPF, SQL Server" Foreground="#A0A0A0" FontSize="14" Margin="0,5,0,10"/>
                        <TextBlock Text="Full-stack LMS with borrowing, reservations, and reporting." Foreground="#D0D0D0" FontSize="16" TextWrapping="Wrap"/>
                        <!-- Add Screenshot Placeholder/Area here later -->
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>