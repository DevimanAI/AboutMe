<UserControl x:Class="AboutMe.Views.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="900"
             Background="Transparent">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Iman Ahmadi" Foreground="White" FontSize="60" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Desktop App Developer | Full-Stack | Hardware/Software Integrator" Foreground="#D0D0D0" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,50" TextWrapping="Wrap" TextAlignment="Center"/>

            <TextBlock Text="Explore My World" Foreground="{StaticResource AccentBrush}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <!-- Home (Current Page) - Could be a non-interactive display or a refresh button -->
                <Button x:Name="HomeImageButton" Click="HomeImageButton_Click" Style="{StaticResource ImageNavButtonStyle}" Margin="15">
                    <StackPanel>
                        <!--<Image Source="/Images/Home.png" Width="100" />-->
                        <TextBlock Text="Home" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryForegroundBrush}" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button x:Name="AboutImageButton" Click="AboutImageButton_Click" Style="{StaticResource ImageNavButtonStyle}" Margin="15">
                    <StackPanel>
                        <!--<Image Source="/Images/About.png" Width="100"/>-->
                        <TextBlock Text="About Me" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryForegroundBrush}" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button x:Name="ProjectsImageButton" Click="ProjectsImageButton_Click" Style="{StaticResource ImageNavButtonStyle}" Margin="15">
                    <StackPanel>
                        <!--<Image Source="/Images/Projects.png" Width="100"/>-->
                        <TextBlock Text="Projects" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryForegroundBrush}" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button x:Name="ContactImageButton" Click="ContactImageButton_Click" Style="{StaticResource ImageNavButtonStyle}" Margin="15">
                    <StackPanel>
                        <!--<Image Source="/Images/Socials.png" Width="100"/>-->
                        <TextBlock Text="Contact" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryForegroundBrush}" FontSize="16"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>